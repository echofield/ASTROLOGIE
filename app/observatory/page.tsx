import AtlasHome from "@/components/atlas/AtlasHome";

// The observatory — the instrument's home (the engraved wheel + THE SKY TONIGHT).
// The front door is Your Sky (/); this room is one quiet link behind it.
export const metadata = { title: "The Observatory — The AstroLab" };

export default function Observatory() {
  return <AtlasHome />;
}
