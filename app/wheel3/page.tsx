import WheelDial from "@/components/landing/WheelDial";

// Prototype direction D3 — GSAP astrolabe dial: drag to spin, inertia + snap,
// click to ease a sign under the pointer.
export const metadata = { title: "The AstroLab — Dial" };

export default function Wheel3Page() {
  return <WheelDial />;
}
