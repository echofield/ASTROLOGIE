import WheelLandingPlate from "@/components/landing/WheelLandingPlate";

// Prototype direction D2 — clip-reveal: the generated art IS the wheel.
export const metadata = { title: "The AstroLab — Wheel (art)" };

export default function Wheel2Page() {
  return <WheelLandingPlate />;
}
